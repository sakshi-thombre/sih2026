# Migrations

This directory is reserved for Alembic migrations once the database
is connected (Phase 2, coordinated with Person D). No Alembic setup
exists yet — this is a structural placeholder only.
